*.elc
*~
.elpa/

# Intermediate documentation artifacts (staged and produced by
# `make info' at the repo root and removed on success; listed
# here so a failed build does not leave them tracked).  The final
# `bookmark-gt.info' and `dir' are committed at the repo root.
bookmark-gt.org
bookmark-gt.texi
